appId: com.pkcs12.converter
productName: PKCS12_Converter
directories:
  output: release
files:
  - dist/**/*
  - package.json
extraResources:
  - from: engines
    to: engines
win:
  target:
    - portable
    - zip
  icon: build/icon.ico
  artifactName: ${productName}-${version}.${ext}

